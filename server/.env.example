# Server port (change to use a different port; ngrok and docs should use the same port)
PORT=4000

# Public base URL (HTTPS). Leave empty until you run scripts/start-ngrok-and-update.js (with ngrok running).
# Must match the URL Signicat redirects to (e.g. https://abc123.ngrok-free.app)
BASE_URL=

# Signicat sandbox OIDC (from Signicat dashboard)
SIGNICAT_ISSUER=https://api.sandbox.signicat.com/oidc
SIGNICAT_CLIENT_ID=your-client-id
SIGNICAT_CLIENT_SECRET=your-client-secret

# Android App Links: package and SHA256 cert fingerprint (no colons)
# Get SHA256: keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android
ANDROID_PACKAGE_NAME=com.smartsense.spidpoc
ANDROID_SHA256_FINGERPRINT=YOUR_SHA256_WITHOUT_COLONS

# Secret used to sign our own JWTs (generate with: openssl rand -hex 32)
APP_JWT_SECRET=your-long-random-secret
